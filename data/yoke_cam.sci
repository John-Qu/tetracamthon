mode(0);
ieee(1);
//----
delta_angle = 1; // in degrees
n = 360/delta_angle;
s = zeros(n+1);
v = zeros(n+1);
a = zeros(n+1);
j = zeros(n+1);
theta = 
belta = zeros(
for theta=0;delta_angle;360;
if theta  
case 

